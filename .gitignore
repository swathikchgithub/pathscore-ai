.env
__pycache__/
*.pyc
.venv*/
.pytest_cache/

# dashboard (Next.js)
dashboard/node_modules/
dashboard/.next/
dashboard/.env*.local
dashboard/*.tsbuildinfo

# drift-check output -- regenerated (with a fresh timestamp) on every run;
# the scheduled workflow uploads it as a build artifact instead of committing it
monitoring/drift_log.jsonl
monitoring/dashboard.md
