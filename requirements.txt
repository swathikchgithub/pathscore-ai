lightgbm>=4.3.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
shap>=0.45.0
pyyaml>=6.0
fastapi>=0.110.0
uvicorn>=0.29.0
faker>=24.0.0
python-dotenv>=1.0.0
joblib>=1.4.0
pytest>=8.0.0
snowflake-snowpark-python>=1.20.0  # only needed for live Cortex mode; mock mode has no dependency on it
requests>=2.31.0  # only needed for live LoRA mode; mock mode has no dependency on it
